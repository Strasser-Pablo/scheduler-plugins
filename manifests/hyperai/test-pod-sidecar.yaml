apiVersion: v1
kind: Pod
metadata:
  name: hyperai-test-pod-sidecar
spec:
  schedulerName: hyperai-scheduler-sidecar
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
