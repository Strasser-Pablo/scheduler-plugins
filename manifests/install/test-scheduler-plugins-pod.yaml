apiVersion: v1
kind: Pod
metadata:
  name: test-scheduler-plugins
  namespace: default
spec:
  schedulerName: scheduler-plugins-scheduler
  containers:
    - name: pause
      image: k8s.gcr.io/pause:3.2
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          cpu: 10m
          memory: 16Mi
